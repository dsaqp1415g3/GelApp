<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.eetac.dsa.dsaqp1415g3.gelapp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="Gelapp"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GelappMainActivity"/>
        <activity android:name=".HeladoDetailActivity" />
        <activity android:name=".CreateGelappActivity" />
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".RankingActivity"/>
        <activity android:name=".aboutActivity"/>

    </application>

</manifest>
